parameters:
  - name: kubernetesServiceConnection
    type: string
  - name: namespace
    type: string
  - name: manifests
    type: object
  - name: imageName
    type: string

steps:
  - bash: |
      echo "Replacing image placeholder in deployment manifests..."
      echo "Target image: ${{ parameters.imageName }}"

      # Create temp directory for modified manifests
      mkdir -p $(Pipeline.Workspace)/k8s-modified

      # Process each manifest file
      for manifest in ${{ parameters.manifests }}; do
        if [ -f "$manifest" ]; then
          filename=$(basename "$manifest")
          echo "Processing $filename..."

          # Replace the image placeholder with actual ECR image
          sed 's|image: app-hola-mundo:latest|image: ${{ parameters.imageName }}|g' "$manifest" > "$(Pipeline.Workspace)/k8s-modified/$filename"

          echo "Modified manifest content:"
          cat "$(Pipeline.Workspace)/k8s-modified/$filename"
        fi
      done
    displayName: 'Prepare Kubernetes Manifests'

  - task: KubernetesManifest@0
    displayName: 'Deploy to Kubernetes'
    inputs:
      action: 'deploy'
      kubernetesServiceConnection: '${{ parameters.kubernetesServiceConnection }}'
      namespace: '${{ parameters.namespace }}'
      manifests: |
        $(Pipeline.Workspace)/k8s-modified/deployment.yaml
        $(Pipeline.Workspace)/k8s-modified/service.yaml

  - task: Kubernetes@1
    displayName: 'Verify Deployment'
    inputs:
      connectionType: 'Kubernetes Service Connection'
      kubernetesServiceEndpoint: '${{ parameters.kubernetesServiceConnection }}'
      namespace: '${{ parameters.namespace }}'
      command: 'get'
      arguments: 'pods -l app=app-hola-mundo'

  - task: Kubernetes@1
    displayName: 'Get Service Info'
    inputs:
      connectionType: 'Kubernetes Service Connection'
      kubernetesServiceEndpoint: '${{ parameters.kubernetesServiceConnection }}'
      namespace: '${{ parameters.namespace }}'
      command: 'get'
      arguments: 'service app-hola-mundo-service'
